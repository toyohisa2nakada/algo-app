{
  "name": "algo-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx browser-sync start --server --index \"algo-app.html\" --files \"*.html, *.js\"",
    "compile": "npx tsc algo-ai.ts --target esnext --module esnext --moduleResolution bundler --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "typescript": "^5.9.3"
  }
}
